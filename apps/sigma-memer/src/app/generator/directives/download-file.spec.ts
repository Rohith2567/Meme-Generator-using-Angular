import { DownloadFile } from './download-file';

describe('DownloadFile', () => {
  it('should create an instance', () => {
    const directive = new DownloadFile();
    expect(directive).toBeTruthy();
  });
});
