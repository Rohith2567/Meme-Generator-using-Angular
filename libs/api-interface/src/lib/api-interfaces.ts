export interface MemeToGenerate {
    data: {
        text0?: {
            text: string;
            font_size?: number;
            font?: string;
        },
        text1?: {
            text: string;
            font_size?: number;
            font?: string;
        }
    }
}